apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: glusterfs-grafana-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ""
  volumeName: gv0-glusterfs-pv